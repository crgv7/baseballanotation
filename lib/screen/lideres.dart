import 'package:flutter/material.dart';

class Lideres extends StatelessWidget {
  const Lideres({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lideres"),
      ),
      body: Container(
        child: Center(
          child: Text("Lideres"),
        ),
      ),
    );
  }
}